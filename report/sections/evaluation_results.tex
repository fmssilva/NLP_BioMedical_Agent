%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%   SECTION 3b — Results and Discussion   %%%%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\subsection{Results and Discussion}
\label{sec:eval_results}

% ══════════════════════════════════════════════════════════════════════════════
% 3.2.1  PHASE 1
% ══════════════════════════════════════════════════════════════════════════════
\subsubsection{Phase 1: Retrieval Results and Discussion}

% ── QUERY FIELD ABLATION ──────────────────────────────────────────────────────
\paragraph{Query Field Ablation:}
Six combinations of the topic fields were tested on 32 train queries using
BM25 with default parameters.
\textbf{Winner: \texttt{topic+question}} --- NDCG@100=0.7833, MAP=0.5843.
The \texttt{narrative} field adds noise (instructions and qualifiers not found
in abstracts); \texttt{topic} alone misses clinical context synonyms.
The \texttt{topic+question} concatenation is locked for all subsequent
experiments.  Figure~\ref{fig:field_ablation} shows the results.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{images/field_ablation.png}
  \caption{Field ablation results --- \texttt{topic+question}
           achieves the highest NDCG@100.}
  \label{fig:field_ablation}
\end{figure}

% ── SPARSE RETRIEVER HYPERPARAMETER TUNING ────────────────────────────────────
\paragraph{Sparse Retriever Hyperparameter Tuning:}
All sweeps use the locked \texttt{topic+question} field.
\textbf{BM25} (24 configurations):
winner $k_1$=1.5, $b$=1.0 --- NDCG@100=0.7989
($\Delta$=+0.0088 vs default); high $b$=1.0 applies full length
normalisation, rewarding longer, more comprehensive abstracts.
\textbf{LM~Dirichlet}
($\mu \in \{50,\ldots,2000\}$):
winner $\mu$=100 --- NDCG@100=0.7706
($\Delta$=+0.0196); the default $\mu$=2{,}000 is $\approx$13$\times$ the
mean document length and over-smooths every abstract.
\textbf{LM~Jelinek-Mercer}
($\lambda \in [0.1, 0.9]$, extended grid):
winner $\lambda$=0.65 --- NDCG@100=0.7754
($\Delta$=+0.0008 over $\lambda$=0.7, within noise).
The individual sweep plots appear in Appendix~\ref{app:sparse_figs}.

% ── DENSE RETRIEVER COMPARISON ────────────────────────────────────────────────
\paragraph{Dense Retriever Comparison:}
MedCPT wins over multi-qa-mpnet, SPECTER-2, and
msmarco-distilbert --- NDCG@100=\textbf{0.8273}
($\Delta$=+0.0825 vs msmarco baseline at 0.7448).
Trained on 23M PubMed click-through pairs, MedCPT is an exact domain match
for our task.  Encoder selection used brute-force exact cosine
(not HNSW) to eliminate index-approximation noise; each encoder's best
pooling mode was determined first (see §\ref{sec:phase1}).
Figure~\ref{fig:encoder_sweep} shows the results.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{images/encoder_comparison.png}
  \caption{Encoder comparison --- NDCG@100 on 32 train topics. MedCPT
           dominates.}
  \label{fig:encoder_sweep}
\end{figure}

% ── RRF SPARSE+DENSE FUSION ──────────────────────────────────────────────────
\paragraph{RRF Sparse+Dense Fusion:}
Nine strategy pairs were tested across three $k$ values (30, 60, 90) in
5-fold CV.  The best lexical pair was BM25+LM-Dir at $k$=90
(NDCG@100=0.7907); however, no RRF pair surpasses KNN(MedCPT) solo
(0.8273) on the train CV leaderboard.
Full pair-sweep plot is in Appendix~\ref{app:rrf_figs}.

% ── MODEL SELECTION RESULTS ──────────────────────────────────────────────────
\paragraph{Model Selection Results:}
Table~\ref{tab:leaderboard} ranks all tuned strategies by NDCG@100
on the train CV; KNN(MedCPT) is the overall winner.

\begin{table}[htbp]
\centering
\caption{Train CV leaderboard --- KNN(MedCPT) is the overall winner.}
\label{tab:leaderboard}
\begin{tabular}{clcc}
  \hline
  \textbf{Rank} & \textbf{Strategy} & \textbf{NDCG@100} & \textbf{MAP} \\
  \hline
  1 & KNN (MedCPT)               & \textbf{0.8273} & \textbf{0.6209} \\
  2 & BM25 ($k_1$=1.5, $b$=1.0)  & 0.7989          & 0.5961 \\
  3 & RRF (BM25+LM-Dir, $k$=90)  & 0.7907          & 0.5846 \\
  4 & LM-JM ($\lambda$=0.65)     & 0.7754          & 0.5636 \\
  5 & LM-Dir ($\mu$=100)         & 0.7706          & 0.5618 \\
  \hline
\end{tabular}
\end{table}

All strategies are then evaluated once on the held-out test set (33 topics)
with all parameters locked (all use \texttt{topic+question}; top-100 results
per query).
KNN\,(MedCPT) achieves
NDCG@100=0.8077, MAP=0.6143, R@100=0.8933
on the test set, making it the locked Phase~2 backbone.
RRF (BM25+MedCPT, $k$=60) pushes the test NDCG@100 further to
0.8245 by fusing complementary lexical and dense signals.
Selecting MedCPT is the single most impactful change overall:
$\Delta$NDCG@100=+0.0501 versus the msmarco KNN baseline on test
($0.7576 \to 0.8077$).
By contrast, BM25 tuning has minimal effect
($\Delta = -0.0014$ on test --- tuning slightly hurt generalisation),
while LM-Dir tuning delivers a clear improvement
($\mu=100$ versus $\mu=2{,}000$: $0.7341 \to 0.7555$, +0.0214).
The R@100 of 0.8933 for KNN\,(MedCPT) covers more than 89\% of relevant
documents, providing a strong evidence pool for Phase~2 generation.
The per-strategy tuning summary, test-set gain plot, and full
baseline-vs-tuned table appear in Appendix~\ref{app:tuning_summary}.

% ── PRECISION-RECALL CURVES AND AP/MAP ANALYSIS ──────────────────────────────
\paragraph{Precision-Recall Curves and AP/MAP Analysis:}
AP ranges widely across topics, revealing high sensitivity to query phrasing
and topic specificity.
The best-performing topic is~170 (isoniazid hepatotoxicity,
AP\,=\,0.8961), whose specific clinical and drug terminology aligns well
with PubMed vocabulary.
The median case is topic~120 (causes of facial numbness, AP\,=\,0.6277),
while the hardest is topic~140 (spontaneous hand fractures, AP\,=\,0.1168),
where the broad, ambiguous query leaves few relevant PMIDs recoverable in
the top~100.
An informal IDF analysis suggests that low-IDF terms such as ``treatment''
and ``patient'' correlate with low AP, whereas disease-specific or
drug/gene-symbol terms tend to produce higher scores.

Mean interpolated PR curves show that RRF (tuned) and KNN(MedCPT) dominate across the full recall range.
The lexical LM variants show steeper early-recall drops, reflecting
weaker semantic matching.  At high recall levels ($>$0.6), RRF
maintains notably higher precision than any single model, illustrating
the benefit of fusing complementary retrieval signals.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{images/pr_curves_single_and_mean.png}
  \caption{Left: per-topic PR curves for KNN(MedCPT) on all 33 test topics.
           Right: mean interpolated PR curves for all strategies.
           RRF (tuned) and KNN(MedCPT) dominate the full recall range.}
  \label{fig:pr_combined}
\end{figure}

Figure~\ref{fig:pr_combined} presents per-topic PR curves alongside
the mean interpolated curves for all strategies.
Individual per-topic and per-strategy plots appear in
Appendix~\ref{app:pr_curves}.

% ══════════════════════════════════════════════════════════════════════════════
% 3.2.2  PHASE 2
% ══════════════════════════════════════════════════════════════════════════════
\subsubsection{Phase 2: Generation Results and Discussion}
We tested multiple parameter settings, prompt templates, and sentence limits per abstract, resulting in 40 different configurations. The results are discussed below.

\paragraph{Sentence Alignment:}
We first evaluated limiting the amount of retrieved sentences to a maximum of 3 per abstract. We initially expected a lower performance than without the limit, which was later confirmed, since not all abstracts are relevant to a query and restricting sentence selection could discard useful information; in cases where there are few relevant (high scoring) abstracts, this introduces noise and reduce coverage. To combat this, we decided to test another configuration with an increased threshold of 10, as can be seen in the Figure~\ref{fig:sentence_alignment_all_3} (left) below.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{images/sentence_alignment_all_3_unlimited.png}
  \caption{Sentence alignment results for all configurations; Left: max. 3 sentences per abstract, Right: unlimited sentences per abstract}
  \label{fig:sentence_alignment_all_3}
\end{figure}

Increasing the threshold allowed highly relevant abstracts that are more focused on a query's domain to contribute more strongly. This led to a clear performance improvement without additional computational cost, since full sentence scoring was already required before this; the results can be visualized above~\ref{fig:sentence_alignment_all_3} (right).

Comparing both configurations, no setup with a 3-sentence limit exceeded 70\% Required sentences, while all unlimited configurations performed better, with every model using prompt template 2 surpassing that level of quality. The unlimited variant also yielded fewer Borderline and Unnecessary sentences, and neither variant produced any Inappropriate sentences, validating the effectiveness of both the selection strategy and prompt design.

Contrary to initial expectations, the second most impactful change was removing the topic and narrative from the prompt, keeping only the question and context. Even though we initially thought lowering temperature and top-p in order to reduce creativity and randomness, this change was not as influential; we concluded that, due to these parameters only refining an already computed probability distribution, it is only natural that a poor prompt cannot be fully rescued by optimal sampling, thus, making prompt design a very important step.

The best configuration (temp. = 1.0, top-p = 0.7, user prompt number 2, unlimited amount of sentences per abstract), achieved 72.64\% Required, 12.30\% Borderline, 15.06\% Unnecessary. After this, we evaluated the reliability of the judge by manually evaluation 3 randomly selected topic ids and their stored answers, and then comparing our results with the Judge's results. Our evaluations mostly agreed, with most of the differences being in the distinction between Borderline and Unnecessary sentences. Below is one of the results of our analysis:

\textbf{Question}: "What is tamsulosin used for?" \textbf{Answer}: "Tamsulosin is a uroselective alpha adrenoceptor antagonist approved for the treatment of lower urinary tract symptoms (LUTS) attributable to benign prostatic hyperplasia (BPH) [PMID 31555556]. It is one of four long-acting alpha 1 blockers approved by the Food and Drug Administration for the treatment of symptomatic LUTS/BPH [PMID 18231614]. Additionally, it is used for the treatment of benign prostatic hypertrophy [PMID 10676828]. Over the last 30 years, its utilization has extended to conditions beyond its original indication [PMID 31555556]."

We evaluated all the sentences as Required, except the last one, which we deemed as Borderline, since in our view, the last sentence partially contributes to the completeness of the answer by providing additional "good-to-know" background context on the medication's usage over the years. The Judge, however, deemed it as Unnecessary. Other examples like this exist, however, we concluded that the LLM is a reliable enough evaluator for a small project like ours, where there are no resources for manually evaluating answers at scale. The full results for this configuration can be seen in the Appendix~\ref{fig:full_sentence_segmentation_best_unlimited}.

\paragraph{Answer Entailment:}
For the answer entailment test, we followed the same evaluation procedure (with and without the amount of sentences threshold, and analytical analysis). The results are evident: having a lower maximum amount of sentences per abstract results in more hallucinations if we cannot retrieve good-enough context. While the amount of Supported answers stayed relatively similar between both cases, there's a positive tradeoff from Unsupported to Partially Supported answers by removing the sentences limitation, both in the case of user prompt template 1 and 2. This can be seen in the Figure~\ref{fig:answer_entailment_all_3_and_unlimited} below.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{images/answer_entailment_all_3_and_unlimited.png}
  \caption{Answer entailment results for all configurations; Left: max. 3 sentences per abstract, Right: unlimited sentences per abstract}
  \label{fig:answer_entailment_all_3_and_unlimited}
\end{figure}

In model configurations with lower temperature and top-p (less creativity/randomness), the use of the second prompt actually reduces the amount of Unsupported answers. But, in models with higher amounts, this prompt has the opposite effect; one possible explanation is that in the second case, the model has a higher randomness with which it tries to overcompensate for the lack of query information by hallucinating some information that is not presented in the query and its context, while in the first prompt it has a much more complete/focused query, that allows for less divergence. The results of answer entailment for the best model can be visualized in the Appendix~\ref{fig:full_answer_entailment_best_unlimited},

In the case of the qualitative analysis, we found that our results aligned much more with the LLM-judge in this test case, having us blindly agree with it much more than in the previous one. In the case of the previous presented query and answer, all the used sentences are correctly passed to or derived from the provided context:
\textbf{PMID 31555556}: mentions that tamsulosin is the single most popular uroselective alpha adrenoceptor antagonist approved for the treatment of LUTS, and that its use has changed from its original one over the last 30 years.
\textbf{PMID 18231614}: mentions that tamsulosin is one of four of the alpha blockers that are the most effective, least costly, and best tolerated of the drugs for relieving LUTS approved by the Food and Drug Administration.
\textbf{PMID 10676828}: references that tamsulosin is used for the treatment of benign prostatic hypertrophy.
We deemed that this answer as fully supported by the context, and so did the judge.

No model is the best in both tests, but considering all the information we have available, we consider that the model with an unlimited amount of sentences, temperature = 1.0, top-p = 0.7, prompt number 2, is the most balanced, providing an equilibrium between the use of relevant sentences and how much the answer is supported by the provided context, while presenting a good quality answer to the user by having smaller randomness but still using the whole vocabulary, weighted, to get the next token. However, the unlimited amount of sentences configuration was only used for comparison, since the project statement explicitly mentions a maximum of 3 sentences per abstract, so, we chose the configuration that adheres to that and has the same parameters, for which we did the same analysis process. Below are this model's final LLM-judge evaluation results in the test set~\ref{fig:final_results_best_model}.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{images/final_results_best_model.png}
  \caption{Sentence alignment and answer entailment test set best model results}
  \label{fig:final_results_best_model}
\end{figure}

On the test set, Required sentence rates remained stable, but Borderline sentences dropped while Unnecessary increased (~12\%/16\% to ~6\%/28\%). This degradation is largely attributed to 5 queries for which no relevant context could be retrieved, partially caused by topic distribution differences between train and test sets. The even/odd topic split strategy proved suboptimal for ensuring balanced difficulty across sets. We also obtained some differences in the answer entailment test due to those same queries for which the model could not correctly retrieve the answer, but which were incorrectly flagged as Unsupported.

% ══════════════════════════════════════════════════════════════════════════════
% 3.2.3  PHASE 3
% ══════════════════════════════════════════════════════════════════════════════
\subsubsection{Phase 3: Agentic Results and Discussion}
To be developed in future work.